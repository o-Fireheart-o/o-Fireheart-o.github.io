## 1단계 - PostgreSQL 설치

```bash
sudo apt update
sudo apt install postgresql postgresql-contrib -y

# 설치 확인법
psql --version
```

## 2단계 - DB생성

```bash
# PostgreSQL 접속
sudo -u postgres psql

# DB와 유저 생성
CREATE DATABASE forma_notify;
CREATE USER flameheart WITH PASSWORD 'zmfltmvl101';
GRANT ALL PRIVILEGES ON DATABASE forma_notify TO flameheart;

# 나가기
\q
```